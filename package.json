{
  "name": "minor-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  }
}
